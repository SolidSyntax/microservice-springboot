spring:
  application:
    name: reporting
  cloud:
    config:
      discovery:
        enabled: true
        service-id: configuration-server

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
